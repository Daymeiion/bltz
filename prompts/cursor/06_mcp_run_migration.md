# Cursor Prompt — Run Supabase migration via MCP

Use the MCP Supabase tool to execute SQL from `supabase/migrations/20251023_create_awards.sql`.
Confirm table creation and indexes exist.
If table already exists, migration should be idempotent (it is).