// Shared test setup. Keep this small — most tests should mock their own
// dependencies inline so failures stay readable.

// Vitest sets NODE_ENV automatically; nothing else is needed for now.
export {};
